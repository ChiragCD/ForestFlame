(fun (print_product n i) (print(* n i)))
(let ((i 1)) (
    loop (
        if (= i 10) (break i) (
            block
                (print_product i input)
                (set! i (+ i 1))
        )
    )
)
)
